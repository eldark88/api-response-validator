#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

basedir=$(dirname "$0")
alias api-response-validator="$basedir/run"

# Do any other automated setup that you need to do here
